@model IEnumerable<_2011600136_ENT.Entities.Transporte>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreServicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TransporteId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreEmpresa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreServicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TransporteId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreEmpresa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServicioTuristicoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServicioTuristicoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServicioTuristicoId })
        </td>
    </tr>
}

</table>
