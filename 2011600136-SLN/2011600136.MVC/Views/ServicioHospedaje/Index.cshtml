@model IEnumerable<_2011600136_ENT.Entities.ServicioHospedaje>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Comida)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bebidas)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Postre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Comida)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Bebidas)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Postre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServicioHospedajeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServicioHospedajeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServicioHospedajeId })
        </td>
    </tr>
}

</table>
