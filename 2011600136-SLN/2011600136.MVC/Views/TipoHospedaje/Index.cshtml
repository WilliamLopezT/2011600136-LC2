@model IEnumerable<_2011600136_ENT.Entities.TipoHospedaje>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreHospedaje)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreHospedaje)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoHospedajeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoHospedajeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoHospedajeId })
        </td>
    </tr>
}

</table>
