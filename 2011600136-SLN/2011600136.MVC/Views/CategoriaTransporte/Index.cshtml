@model IEnumerable<_2011600136_ENT.Entities.CategoriaTransporte>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategoriaTransporteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategoriaTransporteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategoriaTransporteId })
        </td>
    </tr>
}

</table>
