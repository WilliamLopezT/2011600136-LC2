@model IEnumerable<_2011600136_ENT.Entities.CalificacionHospedaje>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescripcionCalificacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescripcionCalificacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CalificacionHospedajeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CalificacionHospedajeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CalificacionHospedajeId })
        </td>
    </tr>
}

</table>
